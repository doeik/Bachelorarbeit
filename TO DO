/procfs

non root user

nicht gast

Dos

cpu, ram, zeit

netzwerk + devices; kein usb, fancontrol

berechtigungen testen (kill etc)

Python script:
    detach stderr
    
    paramter mit json dictionaries übergeben
    socket.makefile()
    tempfile (es gibt auch eine funktion, die directories erstellt)