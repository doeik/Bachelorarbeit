/procfs

non root user

nicht gast

Dos

cpu, ram, zeit

netzwerk + devices; kein usb, fancontrol

berechtigungen testen (kill etc)

Python script:
    detach stderr
    keine umask änderung: setze berechtigungen für socket
    in den code für lxc.LXC_ATTACH_DROP_CAPABILITIES schauen
    
    